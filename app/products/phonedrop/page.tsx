import type { Metadata } from 'next';

export const metadata: Metadata = {
  title: 'PhoneDrop',
  description: 'Fast, direct local file transfer across Android, macOS and Windows.',
};

export { default } from '../../page';
